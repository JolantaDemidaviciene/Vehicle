package Stream.VehicleOwner.Person;

import VehicleClass.VehicleAbstract;

import java.util.ArrayList;

/*public class WriteObjectToCSVFile {
        public WriteObjectToCSVFile(ArrayList<VehicleAbstract>){

       /*     FileWriter fw = null;
            BufferedWriter bw = null;
            try {
                fw = new FileWriter("failas.csv");
                bw = new BufferedWriter(fw);
                bw.write("FirstName;LastName;Age\n");

                for (Person p: allPeople){
                    bw.write(String.format("%s;%s;%s\n", p.getFirstName(), p.getLastName(), p.getAge() ));
                }

            } catch (Exception exc) {
                System.out.println(String.format("Ivyko klaida: %s", exc.getMessage()));
            } finally {
                bw.close();
            }
        }

}
*/